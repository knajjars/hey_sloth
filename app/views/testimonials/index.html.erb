<p id="notice"><%= notice %></p>

<h1>Testimonials</h1>

<table>
  <thead>
  <tr>
    <th>Name</th>
    <th>Company</th>
    <th>Role</th>
    <th>Social link</th>
    <th>Testimonial</th>
    <th>Source</th>
    <th>Showcase</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @testimonials.each do |testimonial| %>
    <tr>
      <td><%= testimonial.name %></td>
      <td><%= testimonial.company %></td>
      <td><%= testimonial.role %></td>
      <td><%= testimonial.social_link %></td>
      <td><%= testimonial.testimonial %></td>
      <td><%= testimonial.source.capitalize %></td>
      <td><%= button_to "#{testimonial.showcase}", toggle_showcase_testimonial_path(testimonial), method: :post %></td>
      <td><%= link_to 'Show', testimonial %></td>
      <td><%= link_to 'Destroy', testimonial, method: :delete, data: { confirm: 'Are you sure?' } %></td>
    </tr>
  <% end %>
  </tbody>
</table>

<br>