<h1>Collect New Testimonial</h1>

<%= form_with(model: @testimonial, url: collect_from_fire_link_create_path) do |form| %>
  <% if @testimonial.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@testimonial.errors.count, "error") %> prohibited this testimonial from being saved:</h2>

      <ul>
        <% @testimonial.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <%= image_tag @fire_link.logo if @fire_link.logo.attached? %>
  <%= content_tag :p, @fire_link.note unless @fire_link.note.empty? %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, required: true %>
  </div>

  <div class="field">
    <%= form.label :company %>
    <% if @fire_link.job_required %>
      <%= form.text_field :company, required: true %>
    <% else %>
      <%= form.text_field :company, required: false %>
    <% end %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <% if @fire_link.job_required %>
      <%= form.text_field :role, required: true %>
    <% else %>
      <%= form.text_field :role, required: false %>
    <% end %>
  </div>

  <div class="field">
    <%= form.label :social_link %>
    <%= form.text_field :social_link, required: true %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <% if @fire_link.image_required %>
      <%= form.file_field :image, required: true, accept: "image/png, image/jpg, image/jpeg" %>
    <% else %>
      <%= form.file_field :image, accept: "image/png, image/jpg, image/jpeg" %>
    <% end %>
  </div>

  <div class="field">
    <%= form.label :rich_text, "Content" %>
    <%= form.rich_text_area :rich_text, required: true %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
